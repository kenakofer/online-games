*client_secret*
# config.py used to hold the Google client secret. It now only reads from the
# environment, so it is tracked; real secrets live in env vars.
*.pyc
*.db
*.wsgi
*.swp
*.back
*.xcf
*.zip
/venv
/venv
/activate
__pycache__
